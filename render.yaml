services:
  - type: web
    name: king-bro-v2
    runtime: node
    rootDir: web
    plan: free
    buildCommand: corepack enable && pnpm install --frozen-lockfile && pnpm build
    startCommand: pnpm start
    autoDeploy: true
    envVars:
      - key: NODE_VERSION
        value: 22.13.0
